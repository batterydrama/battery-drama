name: Build Mac Beta

on:
  workflow_dispatch:

jobs:
  build-mac:
    runs-on: macos-15-intel

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build Mac x64 distributable
        run: npm run make -- --platform=darwin --arch=x64

      - name: Upload Mac build artifact
        uses: actions/upload-artifact@v4
        with:
          name: battery-drama-mac-x64
          path: out/make/**/*.zip